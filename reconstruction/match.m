function [coefficient,offset] = match(A,B)
% matches recordings A and B
%input: recordings A and B
%output: coefficient - strength of match, from [0,1]
%        offset - offset added to B to line up with matched portion of A
%           i.e A(t) ~ B(t + offset)
end

